import Plugin from 'src/plugin-system/plugin.class';
// disable core plugin that adds state country (Bundesland)

export default class CountryStateSelectPlugin extends Plugin {
    init() {
    }
    initSelects() {
    }

    _updateCountryStateSelect() {
    }

    _onCountryChange() {
    }
}