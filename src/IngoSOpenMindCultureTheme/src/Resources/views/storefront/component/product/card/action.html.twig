{% sw_extends '@Storefront/storefront/component/product/card/action.html.twig' %}

{% block component_product_box_action_buy_button %}
    <button class="btn btn-block btn-buy"
            title="{{ 'listing.boxAddProduct'|trans|sw_sanitize }}"
            aria-label="{{ 'listing.boxAddProduct'|trans|sw_sanitize }}">
        {% sw_icon 'cart' %}
    </button>
{% endblock %}

{% block component_product_box_action_detail %}
    <a class="btn btn-block btn-detail"
            title="{{ 'listing.boxProductDetails'|trans|sw_sanitize }}"
            aria-label="{{ 'listing.boxProductDetails'|trans|sw_sanitize }}">
        {% sw_icon 'arrow-head-right' %}
    </a>
{% endblock %}